# Implementation Plans

Generated by the improve skill on 2026-09-04. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001  | Fix CoinSearch async race condition | P1 | S | LOW | — | DONE |
| 002  | Hoist Intl.NumberFormat allocators to module scope | P1 | S | LOW | — | DONE |
| 003  | Extract shared pctChange helper | P1 | S | LOW | — | DONE |
| 004  | Extract shared ReturnBadge component | P2 | S | LOW | — | DONE (4 of 5 sites; DeleteHoldingDialog had no arrow badge — see notes) |
| 005  | Extract useControlledOpen hook | P2 | S | LOW | — | DONE |
| 006  | Memoize PositionRow and stabilize callbacks | P1 | M | MED | — | DONE |
| 007  | Memoize PortfolioSummary aggregations | P2 | S | LOW | — | DONE |
| 008  | Fix usePortfolioHistory stale closure | P1 | M | MED | — | DONE |
| 009  | Remove dead code (formatPercent + table.tsx) | P3 | S | LOW | 002 | DONE |
| 010  | Create PortfolioProvider context | P2 | M | MED | 006 | DONE |

## Dependency notes

- **009 depends on 002**: Plan 002 hoists `formatPercent`'s `Intl.NumberFormat` before plan 009 deletes it. Execute 002 first to avoid editing the same lines.
- **010 depends on 006**: Plan 006 memoizes `PositionRow` in HoldingsLedger. Plan 010 changes HoldingsLedger's props to use context. Execute 006 first to avoid merge conflicts on the same file.
- **All others are independent** and can be executed in any order.

## Recommended batch execution

For maximum parallelism with no conflicts:

**Batch 1** (independent, can run in parallel):
- 001 (CoinSearch race condition)
- 002 (Hoist Intl formatters)
- 003 (pctChange helper)
- 006 (PositionRow memoization)

**Batch 2** (after batch 1, still independent):
- 004 (ReturnBadge component)
- 005 (useControlledOpen hook)
- 007 (PortfolioSummary memo)
- 008 (usePortfolioHistory stale closure)
- 009 (Dead code removal — after 002)

**Batch 3** (after 006):
- 010 (PortfolioProvider context)

## Findings considered and rejected

- **StatDisplay unification** (4 stat tiles): the 4 stat tiles (`StatCard`, `MobileStat`, `Stat`, `SummaryStat`) have genuinely different layouts — PortfolioSummary's `StatCard` has `size`/`hint`/`spanFull` variants, HoldingsLedger's `MobileStat` has a `muted` flag. A shared component would need 6+ variant branches, creating a component that's harder to maintain than the 4 small separate ones. Over-abstraction.
- **PositionRow state consolidation** (8 useState → useReducer): the dialog orchestration (close one, open another) is fragile. The risk of restructuring the state machine outweighs the readability gain. Skip.
- **SWR vs raw useEffect standardization**: changing from raw useEffect to SWR changes timing semantics (SWR deduplicates in-flight requests). High risk for medium payoff. Skip.
- **coingecko.ts split** (365 lines → 4 modules): the serial request queue has subtle timing guarantees (MIN_INTERVAL, 429 retry). Splitting risks breaking throttling behavior. Large effort, medium confidence. Skip for now.
- **AddHoldingDialog split** (395 lines → 3 dialogs): the price-fetching retry loop with progress tracking is complex and must work identically across modes. Splitting is high risk. Skip for now.
